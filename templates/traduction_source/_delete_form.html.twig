<form method="post" action="{{ path('traduction_source_delete', {'id': traduction_source.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traduction_source.id) }}">
    <button class="btn">Delete</button>
</form>
